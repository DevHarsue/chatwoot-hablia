<p>Hi <%= @resource.name.presence || @resource.email %>,</p>

<p>Set up a password to access your Hablia account directly at any time.</p>

<p><%= link_to 'Set my password', frontend_url('auth/password/edit', reset_password_token: @token) %></p>

<p>If you didn't request this, you can safely ignore this email.</p>
